Retornar histórico do preço medio de produtos de cada categoria:
GET /categories/prices
response: {
  list: CategoryPrices[]
}

interface CategoryPrices {
  category: string,
  prices: PriceHistory
}
